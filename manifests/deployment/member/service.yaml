apiVersion: v1
kind: Service

metadata:
  name: grow-member-service

spec:
  type: NodePort
  selector:
    app: grow-member-app
  ports:
    - port: 8081
      protocol: TCP
      targetPort: 8081
      nodePort: 30001
